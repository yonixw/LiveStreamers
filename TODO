

gtm or posthog ping with disable in setting 
    user uuid in data? 
    yoni-livestreamcircles@
    start 
    quit
    beacon every 10m 
        channel count + min/max links per channel 
        which setting key in json? 
    setting save = setting change 

ReadME + screenshot

--------- V2:

Chat triggers
    Kick / Twitch / YT / Destiny
        Websocket for common trigger?
        Rate
        Rate of word - emote
            +1 ? until "read"
        word mentioned
            +1 ? until "read"

Get screenshot from live?
    Video - from random timestamps?

    YT-DLP?

    Chrome tab screenshot after 10sec of waiting to load?
        Muted tab?


